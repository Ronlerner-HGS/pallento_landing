import type { Metadata } from 'next'
import './globals.css'
import { Toaster } from "@/components/ui/toaster"

export const metadata: Metadata = {
  title: 'Pallento - Write Better Notes with Math Equations',
  description: 'Pallento is a rich text editor with built-in block functionality like the /math block that lets you seamlessly type math equations into your notes. Made for students, by students.',
  generator: 'Next.js',
}

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode
}>) {
  return (
    <html lang="en">
      <body>
        {children}
        <Toaster />
      </body>
    </html>
  )
}
